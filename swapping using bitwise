#include <stdio.h>
int main(void) {
int a, b;
printf("\nEnter two numbers: ");
scanf("%d %d", &a, &b);
// logic starts
a = a ^ b;
b = a ^ b;
a = a ^ b;
printf("\nAfter swapping: a = %d, b =%d\n", a, b);
return 0;
}
